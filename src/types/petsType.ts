export interface PetType {
  id?: string; 
  type: "cat" | "dog";
  name: string;
  breed: string;
  gender: string;
  age: string;
  birthDate: string;
  weight: string;
  image: string;
  createdAt: string;
}